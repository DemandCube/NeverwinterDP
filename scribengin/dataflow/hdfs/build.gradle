archivesBaseName = 'scribengin.dataflow.hdfs'

eclipse {
  project {
    name = 'scribengin.dataflow.hdfs'
  }
}

dependencies {
  compile project(':registry/core')
  compile project(':registry/vm')
  compile project(':scribengin/core')
}
